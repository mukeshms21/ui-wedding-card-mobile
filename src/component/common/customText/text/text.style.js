import {StyleSheet} from 'react-native';
import {normalize} from '../../../../helper/screenRatio';

export default StyleSheet.create({
  textStyle: {
    fontSize: normalize(15),
    fontFamily: 'Mulish-Bold',
  },
});
